import { ColorollyDirective } from './colorolly.directive';

describe('ColorollyDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorollyDirective();
    expect(directive).toBeTruthy();
  });
});

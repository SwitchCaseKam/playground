import { CustomValuePropertyDirective } from './custom-value-property.directive';

describe('CustomValuePropertyDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomValuePropertyDirective();
    expect(directive).toBeTruthy();
  });
});

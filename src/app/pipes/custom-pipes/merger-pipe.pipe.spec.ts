import { MergerPipePipe } from './merger-pipe.pipe';

describe('MergerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new MergerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
